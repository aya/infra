ANSIBLE_SSH_PRIVATE_KEY=${PWD}/../parameters/${ENV}/.ssh/ansible@${ENV}_id_rsa
APP=infra
APP_DOMAIN=${ENV}.${DOMAIN}
APP_HOST=${APP}.${APP_DOMAIN}
APP_NAME=${APP}
APP_PATH=/${ENV_SUFFIX}
APP_SCHEME=http
APP_TYPE=infra
APP_URI=${APP_HOST}${APP_PATH}
APP_URL=${APP_SCHEME}://${APP_URI}
DOMAIN=${ENV}
ENV=dist
SSH_DIR=${HOME}/.ssh
SSL_HOSTNAME=${APP_DOMAIN}
